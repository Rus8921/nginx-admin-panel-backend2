server {
    listen 80; // перед 80 должен быть ip сайта
    server_name 100;
    // параметры для сертификата
    // генератор конфига от мозилы
    location / {
        proxy_pass http://100;
    }
}
// проверить через nginx -t -c и скорпить этот файл